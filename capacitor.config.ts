import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.neonvoicestage80251main',
  appName: 'neon-voice-stage-80251-main',
  webDir: 'dist'
};

export default config;
